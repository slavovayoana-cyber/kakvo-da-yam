-- ============================================================
--  „Какво APPна?" — до 3 снимки на пост
--  Изпълни ЕДНОКРАТНО в Supabase → SQL Editor. Безопасно за повторно пускане.
-- ============================================================

-- Масив със снимки (до 3). Старото поле photo_url остава като „първата снимка".
alter table public.feed_posts add column if not exists photo_urls text[];
